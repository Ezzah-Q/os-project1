//logger program, psuedocode

// libraries (DateTimeFormatter, io*)

// start program

// read command line argument
// save argument in string logfile

// create a datetime pattern of particular format
// set the format to the pattern

// open logfile

//loop
    // read line from driver program via stdin
    // if line reads 'QUIT' --> stop accepting lines
    // else
        // action = word before first space
        // message = word's after first space

    // write to log file in <datetime> [action] [message] format

// close logfile

// end program